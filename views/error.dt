extends layout

block content
    h1 Whoops!
    - import std.conv;
    .box.errorbox(style="text-align: center")
        img(src="surlicious.png", style="max-width: 150px", alt="Eyi, the all seeing observer is watching you.") 
        p(style="font-size: 2rem") It seems like Eyi, the All-Seeing Eye found something wrong going on.
        h2= errorInfo.code
        p= errorInfo.message 