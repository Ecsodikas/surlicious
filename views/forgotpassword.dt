extends layout

block content
    h1 Forgot Password
    form(method="post", action="/forgotpassword")
        fieldset
            legend Please enter your email address to receive an email with further instructions
            p 
                label(for="email") Email address 
                input.input(name="email", type="text", required)
            p
                button.button(type="submit") Send email